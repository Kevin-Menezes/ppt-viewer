{
  "name": "pdf-viewer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "index": "node scripts/generate-pdf-index.js",
    "generate-pages-index": "node scripts/generate-pages-index.js",
    "dev": "npm run index && npm run generate-pages-index && next dev",
    "build": "npm run index && npm run generate-pages-index && next build && node -e \"require('fs').writeFileSync('out/.nojekyll','')\"",
    "start": "node -e \"console.log('Static export enabled. Deploy the out/ folder (e.g. GitHub Pages).')\""
  },
  "dependencies": {
    "next": "14.2.3",
    "pdfjs-dist": "3.11.174",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
